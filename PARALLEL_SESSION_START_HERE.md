# 병렬 자동 효과 검토 세션 시작 문서

최종 확인: 2026-08-21 (Asia/Seoul)

이 파일은 새 Codex/IDE 세션에 가장 먼저 전달하는 현재 상태 문서다. 과거 배치의
상세 기록보다 이 문서의 카드 예약과 브랜치가 우선한다.

## 현재 예약 — 가장 먼저 확인

새로 배정할 카드가 없다. D1의 `CB01-AT-006/009`와 D2의
`CB01-AT-022/025`까지 메인에 구현했으며, 읽기 전용 `--rebuild-reviewed`
전체 검토가 453/453장을 통과했다.

일반 읽기 전용 검토는 저장 증거가 낡은 `ST1-011/012`만 제외해 451/453장이다.
두 장은 구현 대상이 아니므로 병렬 세션에 배정하지 않는다. 현재 회귀 기준은
자동 엔진 803개, 전체 프로젝트 912개다. 다음 병렬 세션은 새 카드 구현 대신
룰셋 게시 전 수동 카드 문구 대조나 브라우저 통합 검수처럼 별도 범위를 명시할
때만 만든다.

## 이전 통합 기록

다음 7장은 보조 브랜치의 카드 전용 정의·검토기·테스트를 dirty `main`에 수동
통합했다. 완료 브랜치는 그대로 보존했다.

| 카드 | 보조 커밋 | 능력 / 결정적 시나리오 |
| --- | --- | --- |
| `CRS-AT-021` | `57b21ed` | 3 / 10 |
| `CRS-AT-024` | `f91dfd1` | 3 / 11 |
| `CB01-AT-008` | `94c3f44` | 2 / 14 |
| `CB01-AT-010` | `14ffcfd` | 3 / 13 |
| `PMP-AT-013` | `a7994b9` | 3 / 20 |
| `PMP-AT-019` | `5a90a1a` | 3 / 18 |
| `PMP-AT-022` | `5a05a96` | 3 / 18 |

합계는 20개 능력 그룹과 104개 결정적 시나리오다. 집중 통합 테스트 8개, 자동
엔진 781개, 전체 프로젝트 890개 SQLite 테스트가 모두 통과했다.

그 뒤 메인에서 `PMP-AT-032/036/044`를 9개 능력 그룹, 73개 결정적 시나리오로
완료했다. 현재 회귀 기준은 자동 엔진 782개, 전체 프로젝트 891개이며, 운영 DB에
쓰지 않은 전체 카탈로그 드라이런 결과는 `419 / 453장 검토 완료`, `34장 검토
필요`다.

메인에서 이어서 `PMP-AT-006/007/008`을 6개 능력 그룹, 54개 결정적
시나리오로 완료했다. 체력 반복 결제와 치명적 결제 중단, 거미 토큰당 속도·데미지,
QNA628의 즉시 패배, QNA657의 고정 속도 우선순위를 포함한다. 최신 회귀 기준은
자동 엔진 783개, 전체 프로젝트 892개였다.

메인에서 `CB01-AT-013/014/015`를 11개 능력 그룹, 76개 결정적 시나리오로
추가 완료했다. 뒷면 루멘 카드·예고장·버릴 패·획득할 리스트 기술을 실제
플레이어 선택으로 처리하고 QNA659~666, 672, 686의 사용 조건·콤보·브레이크
재정을 검증했다. 최신 회귀 기준은 자동 엔진 784개, 전체 프로젝트 893개이며,
읽기 전용 전체 카탈로그 드라이런은 `425 / 453장 검토 완료`, `28장 검토
필요`다.

메인에서 이어서 `CB01-AT-026/027/030`을 8개 능력 그룹, 52개 결정적
시나리오로 완료했다. 낙화의 번호 효과 브레이크 내성과 QNA656의 원자적 점프
브레이크, 폭풍운성각 : 삼식의 실제 음/양 선택 및 리커버리 예약 FP,
주영교격의 9속도 캐치와 암살자 대체 효과를 검증했다. 최신 회귀 기준은 자동
엔진 785개, 전체 프로젝트 894개이며, 운영 DB 무수정 전체 드라이런은
`428 / 453장 검토 완료`, `25장 검토 필요`다.

메인에서 충돌하지 않는 대기 카드 `CB01-AT-001/028`을 8개 능력 그룹,
58개 결정적 시나리오로 추가 완료했다. 진•오몽!! 킥!!의 HP 사용 경계,
리커버리 예약 FP, FP 열세 시 12속도 이하 중단 회피와 얼티밋 브레이크 보충,
별의 심판의 회피·상쇄 금지, 실제 `LMI-AT-001` 선택·루멘 배치, QNA673의
같은 콤보 타이밍 비소급, 콤보 데미지 감소를 검증했다.

이후 게시 검증에 능력별 최소 3개·통과·결정성·능력 ID 일치 증거 게이트를
추가했다. 운영 DB의 기존 승인 중 `ST1-011/012`는 상시 기능의 과거 증거 ID가
현재 정의 ID와 달라 엄격한 게시 가능 집계에서는 제외된다. 두 카드는 현재
검토기로 읽기 전용 재검토를 통과했으며, 정확한 두 카드에 `--recheck-reviewed
--apply`를 실행하면 다시 포함된다. 최신 회귀 기준은 자동 엔진 791개, 전체
프로젝트 900개다. 운영 DB 무수정 엄격 드라이런은 현재 `428 / 453장 게시 증거
완료`, `25장 검토 또는 증거 갱신 필요`이며, 위 두 증거를 적용하면 카드 구현
집계는 다시 `430 / 453`, `23장 잔여`가 된다.

완료 브랜치 `review/rfs`의 RFS 20장, `review/awl`의 AWL 6장,
`review/wolf`의 4장은 메인 소스에 이미 카드 전용 블록으로 통합되어 있음을 다시
확인했다. 이어 메인에서 `CB01-AT-011/012`, `PMP-AT-037`을 9개 능력 그룹,
56개 결정적 시나리오로 완료했다. 플래티나 임팩트의 QNA657을 위해 선행 속도
변경을 보존하는 명시적 속도 고정 옵션을 추가하되, QNA500의 서로 충돌하는 동시
효과 우선순위는 유지했다. 33장 묶음 읽기 전용 드라이런, 자동 엔진 793개와 전체
프로젝트 902개 SQLite 테스트가 통과했다.

최신 운영 DB 무수정 전체 드라이런은 `431 / 453장 게시 증거 완료`, `22장 검토
또는 증거 갱신 필요`다. 이 중 `ST1-011/012`는 구현 미완료가 아니라 저장된 과거
증거 ID만 낡은 카드이므로, 실제 구현 잔여는 20장이다. 두 증거를 정확한 카드에만
재적용하면 `433 / 453장`, 구현 잔여 20장이 된다.

메인에서 이어서 `CRS-AT-006/007/008/026`을 11개 능력 그룹, 70개 결정적
시나리오로 완료했다. 실제 패 브레이크 선택과 QNA523 컴뱃센스 FP 타이밍,
QNA669 초성각/기어체인지 우선권, 6~10 및 9 이하 회피의 FP 제외 속도 경계,
QNA384/406/521/619/641의 효과 캐치 우선·타이밍 소실·원본 재사용·캐치 후
배틀 종료 및 Get 스킵을 검증했다. 자동 엔진 795개와 전체 프로젝트 904개
SQLite 테스트가 통과했다.

최신 운영 DB 무수정 엄격 드라이런은 `435 / 453장 게시 증거 완료`, `18장 검토
또는 증거 갱신 필요`다. 이 중 `ST1-011/012`는 과거 증거만 낡았으므로 실제 구현
잔여는 16장이고, 두 증거를 정확한 카드에만 갱신하면 `437 / 453장`이 된다.

## 병렬 결과 통합 완료와 최종 CB01 배치

PMP 묶음과 두 CB01 묶음의 완료 결과를 메인 소스에 카드 전용 블록 단위로
통합했다. 공유 파일 전체 교체나 운영 DB 쓰기는 수행하지 않았다.

| 통합 묶음 | 카드 | 능력 그룹 | 결정적 시나리오 |
| --- | --- | ---: | ---: |
| PMP | `PMP-AT-010/011/043/046/047/049` | 13 | 88 |
| CB01 패시브·복합 | `CB01-PS-001`, `CB01-AT-020/021/029/035/036` | 15 | 93 |

통합된 CB01 여섯 장의 운영 DB 읽기 전용 재생성 드라이런은 6/6 통과했다.
SQLite 자동 엔진 802개와 전체 프로젝트 911개 테스트도 통과했다. 전체 453장
읽기 전용 일반 드라이런은 저장 증거가 유효한 110장과 자동 검토 337장,
합계 447장을 인정하며 다음 6장을 남긴다.

- 저장 증거만 갱신: `ST1-011`, `ST1-012`
- 실제 카드별 구현 필요: `CB01-AT-006`, `CB01-AT-009`,
  `CB01-AT-022`, `CB01-AT-025`

`--rebuild-reviewed` 읽기 전용 드라이런에서는 ST1 두 장도 현재 검토기로
통과해 449/453장이 된다. 운영 적용 권한이 있는 통합 세션에서만 두 카드의
증거를 별도로 갱신하며, 새 병렬 카드 구현에는 배정하지 않는다.

남은 네 장은 충돌을 줄이기 위해 아래 두 worktree로 나눈다.

| 새 병렬 묶음 | 카드 | 수량 |
| --- | --- | ---: |
| D1 · CB01 조건/수치 | `CB01-AT-006`, `CB01-AT-009` | 2 |
| D2 · CB01 사용 조건/복합 | `CB01-AT-022`, `CB01-AT-025` | 2 |

각 세션은 위 네 카드 중 자기 묶음만 수정한다. `ST1-011/012`는 예약 대상이
아니며, 운영 DB에는 `--apply`, seed, publish를 실행하지 않는다.

## 최종 네 장 통합 완료

D1/D2 결과를 바탕으로 공통 엔진의 상시 효과 면역, 효과 기반 Get 합법성 검사,
콤보 도중 선택적 추가 사용권 재개를 보강하고 네 장을 메인에 구현했다.

| 카드 | 능력 그룹 | 결정적 시나리오 |
| --- | ---: | ---: |
| `CB01-AT-006` | 2 | 10 |
| `CB01-AT-009` | 3 | 15 |
| `CB01-AT-022` | 3 | 15 |
| `CB01-AT-025` | 2 | 14 |

정확한 네 장의 운영 DB 읽기 전용 재생성 검토는 4/4, 자동 엔진은 803개,
전체 프로젝트는 912개 테스트를 통과했다. 전체 읽기 전용 검토는 일반
451/453장, `--rebuild-reviewed` 453/453장이다. 따라서 새 카드 번호 목록은
비어 있으며 위 D1/D2 표는 완료 이력으로만 남긴다.

주의: 위 453/453은 읽기 전용 재생성 결과다. 운영 DB 저장본은 아직 검토 완료
112장, 적용 대기 341장이므로 `validate_automatic_ruleset`이 실패한다. 병렬
세션은 이를 적용하지 않으며, 운영 반영은 별도 승인 후 통합 세션에서만 한다.

## 수정 범위와 완료 조건

보조 세션이 수정할 수 있는 파일은 아래 세 개에서 자기 카드 전용 블록뿐이다.

- `LumenGG/battlelog/game/drafts.py`
- `LumenGG/battlelog/game/review.py`
- `LumenGG/battlelog/test_automatic_engine.py`

`engine.py`, `schema.py`, `effects.py`, `spec.py`, 모델, 마이그레이션, API,
JavaScript, 템플릿과 모든 문서는 통합 세션만 수정한다. 공통 지원이 부족하면 카드
구현을 억지로 통과시키지 말고 최소 실패 시나리오를 남긴다.

각 카드의 완료 조건은 다음과 같다.

- 카드 원문, 보충 설명, 카드별 Q&A와 일반 Q&A 우선순위를 확인한다.
- 능력 그룹마다 최소 3개 결정적 시나리오를 둔다.
- 양쪽 소유자, 성공, 조건 경계/실패, 번호 효과 무효를 포함한다.
- 실제 선택이 필요한 이동은 후보 없음·복수 후보·선택 뒤 무효화도 검증한다.
- 카드별 드라이런과 자동 엔진 SQLite 전체 테스트를 통과한다.
- 카드 한 장마다 커밋 하나만 만들고 커밋 해시를 전달한다.

## 충돌 방지 규칙

1. 한 worktree를 둘 이상의 세션이 동시에 열지 않는다.
2. 예약 카드 밖의 코드를 수정하지 않는다.
3. 보조 세션은 `git pull`, `merge`, `rebase`, `stash`를 실행하지 않는다.
4. `git add -A` 대신 허용된 실제 변경 파일만 명시적으로 스테이징한다.
5. 운영 DB에는 쓰지 않는다. `--apply`, seed, publish를 사용하지 않는다.
6. SQLite 테스트는 병렬 실행할 수 있지만 같은 MariaDB 테스트 DB는 공유하지 않는다.
7. `.temp`, `test.local.ps1`, 비밀 키와 DB 자격 증명을 복사하거나 공유하지 않는다.
8. 통합은 메인 세션 한 곳에서만 수행한다. 공유 파일 전체를 어느 브랜치 버전으로
   교체하지 않고 카드별 블록만 병합한다.

## 완료 보고 형식

```text
카드/작업:
기준 HEAD와 작업 브랜치:
해석 요약:
변경 파일:
능력 그룹/결정적 시나리오 수:
카드별 읽기 전용 드라이런:
SQLite 자동 엔진 테스트:
git diff --check:
커밋 해시:
남은 재정 의문:
필요한 공통 엔진 변경:
```
